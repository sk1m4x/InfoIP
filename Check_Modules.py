#!/usr/bin/python
# -*- coding: utf-8 -*-

# InfoIP Project

# Official Repository:
# https://github.com/sk1m4x/InfoIP/

try:
	import sys
except ImportError:
	print "sys is not installed."

try:
	import json
except ImportError:
	print "json is not installed."

try:
	import requests
except ImportError:
	print "requests is not installed."

try:
	import socket
except ImportError:
	print "socket is not installed."

try:
	import os
except ImportError:
	print "os is not installed."

try:
	import time
except ImportError:
	print "time is not installed."
